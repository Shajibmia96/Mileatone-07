// import React from 'react';

const Example = ({children}) => {
    return (
        <div>
             <h1>This is my Example pet</h1>
              {children}
        </div>
    );
};

export default Example;